export interface cmFieldPropType {
  form: any;
  name: string;
  label?: string;
  tipText?: string;
  type?: string;
  placeholder?: string;
  className?: {
    formItem: string;
    label: string;
    message: string;
    input: string;
  };
  bottom?: any;
}
